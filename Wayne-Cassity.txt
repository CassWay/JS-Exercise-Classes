Reference, Notes and -- commits 
-- initial commit; packages installed, task1 - Person; -- look over someFood and toString
-- Car constructor built; -- previous someFood and toString refactored & working
-- car fill and drive built; -- no issues 
-- Lamdasian built; -- no issues
-- Instructor extends built; -- no issues
-- Student extends built to listSubject; -- all working -- end here for night
-- Student extends built to sprintChallenge; -- this.name not Student
-- ProjectManager extends built; adjusted punctuation; --no issues
-- ALL PASSED; reviewed and adjusted punctuation; -- No Issues, -- DO STRETCH
-- ALL PASSED; Added STRETCH -- No issues